{
  "name": "cinemashowtimes",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "@types/typescript": "^2.0.0",
    "express": "^4.19.2"
  },
  "scripts": {
    "back-build": "cd private && tsc",
    "back-dev": "npm run back-build && nodemon --env-file=.env private/dist/index.js",
    "back-prod": "npm run back-build && node --env-file=.env private/dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^20.12.12",
    "nodemon": "^3.1.1"
  }
}
